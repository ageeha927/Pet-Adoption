<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Pet</title>
    <!-- For some reason my css won't work unless its in the ejs -->
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Helvetica Neue', sans-serif;
        }

        body {
            background-color: #f2f2f7;
            color: #000;
            line-height: 1.4;
            background-color: rgb(249, 250, 251);
            max-width: 428px; /* iPhone 14 width */
            margin: 0 auto;
            min-height: 100vh;
            padding: 24px 24px 24px;
        }

        .header {
            padding: 16px;
            font-size: 17px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .form-group {
            background-color: #fff;
            border-radius: 10px;
            margin-bottom: 16px;
            padding: 12px 16px;
        }

        label {
            display: block;
            font-size: 15px;
            color: #666;
            margin-bottom: 4px;
        }

        input[type="text"],
        input[type="number"],
        textarea,
        select {
            width: 100%;
            border: none;
            font-size: 17px;
            background: transparent;
            outline: none;
            padding: 4px 0;
        }

        textarea {
            resize: none;
            min-height: 80px;
        }   

        select {
            -webkit-appearance: none;
            -moz-appearance: none;
            appearance: none;
            background: url("https://cdn4.iconfinder.com/data/icons/icon-flat-icon-set/50/triangle-down-512.png") no-repeat right 8px center;
            background-size: 12px;
            padding-right: 24px;
        }

        .image-upload {
            background: #fff;
            border-radius: 10px;
            padding: 16px;
            text-align: center;
            margin-bottom: 16px;
        }

        .image-upload-icon {
            width: 40px;
            height: 40px;
            border: 2px solid #3579BE;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 8px auto;
            font-size: 24px;
        }

        input[type="file"] {
            display: none;
        }

        .file-label {
            color: #3579BE;
            cursor: pointer;
        }

        button[type="submit"] {
            background-color: #3579BE;
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 14px;
            width: 100%;
            font-size: 17px;
            font-weight: 600;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="header">
        <span>🐾</span>
        <h1>Edit Pet</h1>
    </div>

    <form action="/editPet/<%= pet._id %>" method="post" enctype="multipart/form-data">
        <div class="form-group">
            <label for="name">Pet Name</label>
            <input type="text" name="name" id="name" value="<%= pet.name %>" required>
        </div>

        <div class="form-group">
            <label for="breed">Breed</label>
            <input type="text" name="breed" id="breed" value="<%= pet.breed %>" required>
        </div>

        <div class="form-group">
            <label for="age">Age</label>
            <input type="number" name="age" id="age" value="<%= pet.age %>" required>
        </div>

        <div class="form-group">
            <label for="description">Description</label>
            <textarea name="description" id="description" required><%= pet.description %></textarea>
        </div>

        <div class="form-group">
            <label for="behavior">Behavior</label>
            <textarea name="behavior" id="behavior" required><%= pet.behavior %></textarea>
        </div>

        <div class="form-group">
            <label for="history">History</label>
            <textarea name="history" id="history" required><%= pet.history %></textarea>
        </div>

        <div class="form-group">
            <label for="type">Type</label>
            <select name="type" required>
                <option value="Snake" <%= pet.type === 'Snake' ? 'selected' : '' %>>Snake</option>
                <option value="Lizard" <%= pet.type === 'Lizard' ? 'selected' : '' %>>Lizard</option>
                <option value="Frog" <%= pet.type === 'Frog' ? 'selected' : '' %>>Frog</option>
            </select>
        </div>

        <div class="form-group">
            <label for="gender">Gender</label>
            <select name="gender" required>
                <option value="Male" <%= pet.gender === 'Male' ? 'selected' : '' %>>Male</option>
                <option value="Female" <%= pet.gender === 'Female' ? 'selected' : '' %>>Female</option>
            </select>
        </div>

        <div class="form-group">
            <label for="location">Location</label>
            <select name="location" required>
                <option value="North West" <%= pet.location === 'North West' ? 'selected' : '' %>>North West</option>
                <option value="West" <%= pet.location === 'West' ? 'selected' : '' %>>West</option>
                <option value="South West" <%= pet.location === 'South West' ? 'selected' : '' %>>South West</option>
                <option value="Mid West" <%= pet.location === 'Mid West' ? 'selected' : '' %>>Mid West</option>
                <option value="South East" <%= pet.location === 'South East' ? 'selected' : '' %>>South East</option>
                <option value="Mid Atlantic" <%= pet.location === 'Mid Atlantic' ? 'selected' : '' %>>Mid Atlantic</option>
                <option value="North East" <%= pet.location === 'North East' ? 'selected' : '' %>>North East</option>
            </select>
        </div>

        <div class="image-upload">
            <div class="image-upload-icon">↑</div>
            <label for="image" class="file-label">Choose Image</label>
            <input type="file" name="image" id="image" accept="image/*">
        </div>

        <button type="submit">Update Pet</button>
    </form>
</body>
</html>